# Copy to .env.local (which is gitignored) and fill in.
# Both variables come from the Supabase dashboard:
#   Project Settings → API → Project URL / Project API keys → anon public
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
